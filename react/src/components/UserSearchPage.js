import React from 'react';
import { Link } from 'react-router';

const UserSearchPage = ({prop1}) => {
  return (
    <div>
      <h1>User Search Page</h1>
    </div>
  );
};

export default UserSearchPage;
